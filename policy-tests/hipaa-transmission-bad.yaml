application:
  name: phi-portal
  security:
    transmission:
      enforce_tls: false         # VIOLATION: TLS not enforced
      min_tls_version: "1.0"     # VIOLATION: weak TLS version
      allow_http: true           # VIOLATION: HTTP allowed
      cipher_suites:
        - "TLS_RSA_WITH_RC4_128_SHA"  # VIOLATION: weak cipher